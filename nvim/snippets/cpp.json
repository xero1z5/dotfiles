{
  "CP Template": {
    "prefix": "code",
    "body": [
        "#include <bits/stdc++.h>",
        "",
        "using namespace std;",
        "",
        "#define fastio() ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)",
        "#define nl \"\\n\"",
        "#define pb push_back",
        "#define pk pop_back",
        "#define ff first",
        "#define ss second",
        "#define full(x) (x).begin(), (x).end()",
        "#define fo(i,a,b) for(decltype(b) i=a;i<b;i++)",
        "#define fob(i,a,b) for(auto i=a;i>=b;i--)",
        "#define sortall(x) sort(full(x))",
        "#define PI 3.1415926535897932384626",
        "#define INF 1e18",
        "#define mod 1000000007",
        "#define yes cout<<\"YES\"<<nl",
        "#define no cout<<\"NO\"<<nl",
        "",
        "typedef long long ll;",
        "typedef long double lld;",
        "typedef pair<int, int> pi;",
        "typedef pair<ll, ll> pl;",
        "typedef vector<int> vi;",
        "typedef vector<ll> vl;",
        "typedef vector<pi> vpi;",
        "typedef vector<pl> vpl;",
        "typedef vector<vi> vvi;",
        "typedef vector<vl> vvl;",
        "",
        "//*debug  -->  Using LLDB now",
        "#ifdef DEBUG",
        "#include \"../debug.h\"",
        "#else",
        "#define debug(...)",
        "#endif",
        "",
        "//*overloads",
        "template<typename T>",
        "istream& operator>>(istream &istream, vector<T> &v){for (auto &it : v)cin >> it;return istream;}",
        "template<typename T>",
        "ostream& operator<<(ostream &ostream, const vector<T> &c) { for (auto &it : c) cout << it << \" \"; return ostream; }",
        "",
        "//*fxns",
        "template<typename...T>",
        "void read(T&... args) {((cin>>args),...);}",
        "template<typename T>",
        "bool even(T x){return !(x&1);}",
        "",
        "int popcnt(int x) { return __builtin_popcount(x); }",
        "int popcnt(ll x) { return __builtin_popcountll(x); }",
        "int popcnt_mod_2(int x) { return __builtin_parity(x); }",
        "int popcnt_mod_2(ll x) { return __builtin_parityll(x); }",
        "int topbit(int x) { return (x == 0 ? -1 : 31 - __builtin_clz(x)); }",
        "int topbit(ll x) { return (x == 0 ? -1 : 63 - __builtin_clzll(x)); }",
        "int lowbit(int x) { return (x == 0 ? -1 : __builtin_ctz(x)); }",
        "int lowbit(ll x) { return (x == 0 ? -1 : __builtin_ctzll(x)); }",
        "",
        "template<typename T>",
        "T binpow(T a,T b,T m=1ll){a%=m;T res=1;while(b>0){if(b&1){res=res*a%m;}a=a*a%m;b>>=1;}return res;}",
        "",
        "template<typename T> //? smallest id>=x",
        "T upperBound(vector<T> arr,T l,T r,T x) {T id=-1;while(l<=r) {T mid=l+(r-l)/2;if(arr[mid]>=x) {id=mid;r=mid-1;}else{l=mid+1;}}return id;}",
        "",
        "template<typename T> //? largest id<=x",
        "T lowerBound(vector<T> arr,T l,T r,T x) {T id=-1;while(l<=r){T mid=l+(r-l)/2;if(arr[mid]<=x){id=mid;l=mid+1;}else{r=mid-1;}}return id;}",
        "",
        "//*_______________________________________________*//",
        "",
        "void solve(int tc) {",
        "",
        "    $0",
        "",
        "}",
        "",
        "int main() {",
        "    fastio();",
        "    int t; read(t);",
        "    int tc = t;",
        "    while(t--) {",
        "        solve(tc - t);",
        "    }",
        "    return 0;",
        "}"
    ],
    "description": "Competitive programming C++ template with 4-space indentation"
  }
}

